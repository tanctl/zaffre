{
  "name": "example-ts",
  "version": "0.1.0",
  "private": true,
  "type": "commonjs",
  "scripts": {
    "build:wasm": "cd ../../../zaffre-sdk-js && mkdir -p ../tmp ../target-wasm && TMPDIR=../tmp CARGO_TARGET_DIR=../target-wasm wasm-pack build --target nodejs --release --no-opt",
    "test": "npm run build:wasm && tsx --test zaffre.test.ts"
  },
  "devDependencies": {
    "@coral-xyz/anchor": "^0.30.1",
    "@solana/web3.js": "^1.98.0",
    "tsx": "^4.19.2",
    "typescript": "^5.6.3"
  }
}
