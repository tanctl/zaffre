

[package]
name = "verifier-bin"
version = "0.1.0"
edition = "2021"
publish = false

[lib]
crate-type = ["cdylib", "lib"]

[dependencies]
solana-program = "=1.18.26"
blake3 = { version = "1.8.2", default-features = false, features = ["pure"] }
gnark-verifier-solana = { path = "../verifier-lib" } 

[build-dependencies]
gnark-verifier-solana = { path = "../verifier-lib" }

[features]
custom-heap = []
custom-panic = []

[lints.rust]
unexpected_cfgs = { level = "warn", check-cfg = ['cfg(target_os, values("solana"))'] }
